jQuery(function($){

  (function ( app, undefined ) {

    var theApp = {
      init : function() {

      }
    }
    $.extend(app, theApp);

  })( window.MyApp = window.MyApp || {} );

  MyApp.init();

})